# Hero model

The landing hero loads:

```text
public/models/digital-head.glb
```

The current file is a lightweight generated bust mesh. It can be replaced with a compressed,
licensed human head or bust GLB that uses ordinary mesh geometry. The hero falls back to a
procedural bust if this asset is unavailable.
